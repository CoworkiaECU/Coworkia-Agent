import { detectLanguage, getUserLanguage, SUPPORTED_LANGUAGES } from './src/utils/language-detector.js';

console.log('\n🌍 TEST: Sistema de Detección de Idiomas\n');
console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n');

const testCases = [
  // Español
  { msg: 'Hola, ¿cómo estás?', expected: 'es', desc: 'Español con signos' },
  { msg: 'Quiero reservar una sala para mañana', expected: 'es', desc: 'Español reserva' },
  { msg: 'Buenos días, necesito información', expected: 'es', desc: 'Español formal' },
  
  // English  
  { msg: 'Hello, how are you?', expected: 'en', desc: 'English greeting' },
  { msg: 'I want to book a room for tomorrow', expected: 'en', desc: 'English booking' },
  { msg: 'Good morning, I need information', expected: 'en', desc: 'English formal' },
  
  // Francés (debería detectar como inglés o español según siimport { detectLanguage, getUserLanguage, SUPPORTED_LANGUAGES } from './'F
console.log('\n🌍 TEST: Sistema de Detección de Idiomas\n');
console.log('━━━━━━━━?;
cons passed = 0;
let failed = 0;

testCases.forEach((test, i) =>
const testCases = [
  // Español
  { msg: 'Hola, ¿cómo estás?', expected: 'es', desc: 'Español con signos' },
  { msg: 'Quiero rese.ms  // Español
  { og  { msg: 'Hodo  { msg: 'Quiero reservar una sala para mañana', expect   Confianza: ${(result.c  { msg: 'Buenos días, necesito información', expected: 'es', desc: 'Español formal' },
 '}  
  // English  
  { msg: 'Helquier detección es válida ya que no está soportado
  if ( es  { msg: 'Hel=   { msg: 'I want to book a room for tomorrow', expected: 'en', desc: 'Engles  { msg: 'Good morning, I need information', expected: 'en', desc: 'English formal'  conso  
  // Francés (debería detectar como inglés o español según siimport { detectLo  {tconsole.log('\n🌍 TEST: Sistema de Detección de Idiomas\n');
console.log('━━━━━━━━?;
cons passed = 0;
let failed = 0;

??console.log('━━━━━━━━?;
cons passed = 0;
let ??ons passed = 0;
let failed = 0;

testCUMlet failed = 0;og
testCases.for━const testCases = [
  // Espa?? // Español
  { ??  { msg: 'Ho? { msg: 'Quiero rese.ms  // Español
  { og  { msg: 'Hodo  { msg: 'Quiero reseas  { og  { msg: 'Hodo  { msg: 'Quierodo '}  
  // English  
  { msg: 'Helquier detección es válida ya que no está soportado
  if ( es  { msg: 'Hel=   { msg: 'I want to book a room for tomorrow', expected: 'en', desc: 'Engles  
c  //le  { msg: 'Helna  if ( es  { msg: 'Hel=   { msg: 'I want to book a room for tomorFr  // Francés ( implementado');
console.log('   Sistema detectará el idioma más similar (es/en)\n');
